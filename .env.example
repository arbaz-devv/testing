# Database (same as cryptoi – use one Postgres DB)
POSTGRES_PRISMA_URL="postgresql://user:password@localhost:5432/cryptoi"

# JWT secret for session tokens. REQUIRED in production (min 32 chars). Omit in dev for default.
# JWT_SECRET=your-at-least-32-character-secret-in-production

# Optional: Port (default 8000)
PORT=8000

# Optional: CORS origin for frontend (e.g. http://localhost:3000). Leave unset to allow any origin.
# CORS_ORIGIN=http://localhost:3000
